# MiMo 256K / two-request deployment, 2026-09-22

The later [required-tool-argument repair](2026-09-22-mimo-required-tools.md) supersedes the
binary/deployment paths below while retaining these capacity settings.

User-requested configuration change from 64K/eight requests to 262,144 tokens
per request and two concurrent requests, with exactly four prefix snapshots.
Port remains 30001 on Spark-1 (192.168.0.171); Spark-2 is 192.168.0.172.
The token limit includes input and generated output.

## Allocation and deployment

`prefix_cache_gib=11.297607421875` is exactly four 3,032,678,400-byte snapshots.
The existing byte-budget interface floors to slots; both ranks report four.

| Planned allocation per rank | GiB |
|---|---:|
| Resident text weights | 81.11 |
| Active global/ring KV | 6.39 |
| Layer scratch | 0.76 |
| Session core | 0.62 |
| Shared attention workspace | 6.00 |
| Prefix snapshots | 11.30 |
| Engine | 0.07 |
| Total | 106.24 |

Both ranks use digest `dfd06bf77fbc5019`, namespace `164b183c9ee645c9`,
with graphs enabled, MTP disabled and busy/idle prefill budgets 256/2048.
Both binary hashes match the previous verified implementation:
`15b613c0e119a3a2dffbc56a2cc7b9133402fd6aa94ec622a8739d5a90ec5e1a`.
Rank PIDs at deployment are 3242125 / 2010678. READY was 08:57:22 UTC.
Available node memory after startup was 8.26 / 8.06 GiB.
This change adjusts configuration only; it does not change inference arithmetic.

Config on Spark-1:
`/home/jon/dgpp/experimental/mimo-256k-c2-20260922/deployment.json`.
Binary: `/home/jon/dgpp/experimental/mimo-packed-cache-20260922/dgpp-serve`.
The deployment template and current architecture documentation reflect these
settings. The earlier optimization report retains historical measurements.

## Validation

Raw evidence and scripts: `/tmp/dgpp-mimo-256k-20260922/`.
API reasoning, streaming usage, forced tool calling and two simultaneous
arithmetic requests pass. A short prompt with a 262,000-token output budget
is admitted and stops normally; prompt plus 262,144 requested output tokens
is rejected with `context_length_exceeded`.

Two concurrent conversations return their correct distinct words and both
restore 955 cached tokens on the follow-up. Follow-up latencies are 0.689 and
0.970 seconds. This checks four-slot cache reuse, not indefinite retention.

A 66,030-token prompt (above the old 65,536 limit) returns the correct secret
word: cold first content 234.009 seconds, total 234.238 seconds. Its repeat
restores 66,025 tokens and returns the same answer: first content 0.337 seconds,
total 0.567 seconds. Raw: `long.json`. A full 256K prompt has not been exercised;
this establishes configured capacity, budget admission and crossing the former
limit, not full-capacity performance or numerical equivalence.

Final health is OK, with no active/queued requests, no failed requests and no
engine failure. Both rank logs contain no ERROR/FATAL records. Available memory
after checks is 7,593 MiB on Spark-1 and 8,294 MiB on Spark-2 (7.42 / 8.10 GiB).
The service is left running with the requested configuration.

## Operations

Run on Spark-1 under the maintenance lock:

```bash
DGPP_ENV_FILE=/home/jon/dgpp/releases/dgpp-0.1.0+gf1b05f544293.dirty/.env \
python3 /home/jon/dgpp/releases/dgpp-0.1.0+g116d3c375d87/scripts/dgpp-cluster up \
  --config /home/jon/dgpp/experimental/mimo-256k-c2-20260922/deployment.json \
  --bin /home/jon/dgpp/experimental/mimo-packed-cache-20260922/dgpp-serve
```

Use the same arguments with `down` for a coordinated stop. The previous
64K/eight-request config remains preserved at
`/home/jon/dgpp/experimental/mimo-cache16-20260922/deployment.json`.
